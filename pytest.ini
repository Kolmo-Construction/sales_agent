[pytest]
pythonpath = .
addopts = -p no:deepeval
markers =
    safety: Safety gate tests — run first, block all non-safety tests on failure
    requires_ollama: Tests that call Ollama — skipped when Ollama is unreachable
    requires_qdrant: Tests that call Qdrant — skipped when Qdrant is unreachable
