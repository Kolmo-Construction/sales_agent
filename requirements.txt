# Core
pydantic>=2.0

# LLM providers
ollama>=0.4.0                        # local dev — Ollama 0.4+ required for JSON schema → GBNF (CFG)
anthropic>=0.40.0                    # production — Claude API

# CFG-constrained generation (optional — OutlinesProvider only)
# outlines>=0.1.0                    # full EBNF grammar control, loads GGUF directly
# llama-cpp-python>=0.2.0            # required by outlines llamacpp backend

# Vector store + embeddings
qdrant-client[fastembed]>=1.9.0      # includes FastEmbed for local CPU embeddings

# Agent orchestration + state
langgraph>=0.2.0
langgraph-checkpoint-postgres>=1.0.0
psycopg[binary]>=3.1                 # PostgreSQL driver for LangGraph checkpointer

# Evaluation framework
deepeval>=1.0.0                      # test harness, LLM-as-judge, CI gates
ragas>=0.1.0                         # RAG-specific metrics (faithfulness, context precision)
scikit-learn>=1.3                    # classification + retrieval metrics
arize-phoenix>=4.0.0                 # pipeline tracing per stage

# Utilities
python-dotenv>=1.0                   # .env loading
tqdm>=4.0                            # progress bars in ingestion/embedding scripts
